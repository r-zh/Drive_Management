# -*- coding: utf-8 -*-

# All the AI trainning se

# AI works for take the right action in each moment

import numpy as np
import random
import os # init the model for further re-usage
import torch # handle dynamic graphs
import torch.nn # neural network
import torch.nn.functional